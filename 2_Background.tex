\section{Background}

\begin{figure}[t]
    \centering
    % Ensure you have this file or use a placeholder
    \includegraphics[width=0.9\columnwidth]{Figures/edge_back1.png}
    \caption{The overall architecture of \EdgeQuantum. It utilizes a 4-buffer UVM pipeline to stream state vector chunks between NVMe storage and the GPU.}
    \label{overall_architecture}
\end{figure}

\subsection{GPU-Accelerated State Vector Simulation}
Several approaches have been designed to simulate quantum circuits on classical hardware, offering trade-offs between scalability, accuracy, and computational complexity~\cite{qsim, svsim, suzuki2021qulacs, villalonga2019flexible}. While tensor network contraction reduces memory usage by avoiding explicit storage of the state vector, its performance decreases for deep and highly entangled circuits. Its computational cost grows exponentially with entanglement entropy~\cite{gray2018quimb, lykov2022tensor, pan2022simulation}. In contrast, full state vector simulation represents the quantum state as a complex vector of size $2^n$, where $n$ is the number of qubits. It captures superpositions and entanglements without approximation and provides exact amplitude information for validation and fidelity verification~\cite{guerreschi2020intel, de2007massively, qsim, suzuki2021qulacs}. This work focuses on full state vector simulation.

\noindent\textbf{GPU execution model. }Modern simulators such as cuStateVec~\cite{bayraktar2023cuquantum} and Qiskit Aer~\cite{qiskit} are designed around GPU acceleration. The full state vector is allocated in GPU VRAM, and the circuit is processed layer by layer. For each layer, CUDA kernels sweep the state vector and apply quantum gate matrices to the amplitudes. Because data resides on the device, CUDA cores execute updates in parallel without intermediate transfers.

\noindent\textbf{The memory wall. }GPU-accelerated simulation is limited by memory capacity. The memory requirement grows exponentially with the number of qubits. The complexity is $\mathcal{O}(2^n)$, and each complex amplitude requires 16 bytes in double precision or 8 bytes in single precision. A 30-qubit circuit requires 16~GB, exceeding the 8~GB limit of edge devices such as the NVIDIA Jetson Orin Nano. A 34-qubit circuit requires 256~GB, and a 39-qubit circuit requires 4~TB, exceeding DRAM capacity. Standard GPU simulators assume the full state vector fits in VRAM, so larger circuits cause Out-of-Memory failures.
To address this, tiered memory management, asynchronous I/O scheduling, and lossless compression are required. \EdgeQuantum targets scalable full state vector simulation on resource-constrained edge devices.

\subsection{Memory Architecture in Edge Computing}

To effectively handle data-intensive applications like quantum simulation on edge devices, it is essential to understand the underlying memory architecture.
Unlike HPC clusters that utilize discrete CPUs and GPUs connected via PCIe, edge devices such as the NVIDIA Jetson series employ a System-on-Chip (SoC) design with a Unified Memory Architecture (UMA)~\cite{nvidia2022jetson}.
In this architecture, the CPU and GPU share a single physical DRAM pool, eliminating the need for data transfers over a PCIe bus.
However, despite this architectural advantage, the total memory capacity remains a hard bottleneck (e.g., 8~GB to 64~GB), restricting the maximum number of qubits that can be simulated in-memory.

\begin{figure}[t]
    \centering
    \begin{subfigure}[b]{\columnwidth}
        \centering
        \includegraphics[width=\linewidth]{Figures/edge_back2.png}
        \caption{Unified DRAM (LPDDR5).}
        \label{fig:bg_bw_dram}
    \end{subfigure}

    \vspace{0.5em}

    \begin{subfigure}[b]{\columnwidth}
        \centering
        \includegraphics[width=\linewidth]{Figures/edge_back3.png}
        \caption{External NVMe SSD.}
        \label{fig:bg_bw_nvme}
    \end{subfigure}
    \vspace{-.2cm}

    \caption{Bandwidth across the edge memory hierarchy on the Jetson Orin Nano. Even within the unified LPDDR5, access bandwidth varies by ~4$\times$ depending on the access path, and drops by another 18-400$\times$ when spilling to the external NVMe SSD.}
    \label{fig:bg_bandwidth}
    \vspace{-.2cm}
\end{figure}



Figure~\ref{fig:edge_memory_hierarchy} illustrates the memory hierarchy of a typical edge device.
As depicted, the hierarchy consists of three tiers: on-chip caches, unified DRAM, and external NVMe storage.
The unified DRAM offers high bandwidth (e.g., up to 204~GB/s on Jetson Orin) shared between the CPU and GPU.
In contrast, external NVMe storage provides significantly lower bandwidth (e.g., 3-6~GB/s) and higher latency.
Standard simulation frameworks such as \textit{Qsim}~\cite{qsim} and \textit{cuStateVec}~\cite{bayraktar2023cuquantum} typically rely on the operating system's virtual memory management (OS-native swapping) to handle data larger than physical RAM.
However, this approach suffers from severe performance degradation due to I/O thrashing and the semantic gap between the GPU driver and the OS file system.
Specifically, when a GPU kernel accesses a page not present in physical memory, it triggers a page fault that blocks execution until the OS fetches data from the disk.
Since the OS is unaware of the GPU's memory access patterns, it often makes suboptimal eviction decisions, leading to high latency and low GPU utilization.

To address these problems, recent studies in large-scale data processing have proposed out-of-core mechanisms~\cite{lin2022hm, hwang2020centaur} that explicitly manage data movement between storage and GPU memory.
These approaches typically employ a user-space buffer manager to prefetch data and overlap I/O with computation.
However, applying these techniques to quantum simulation on unified memory architectures presents unique challenges.
Existing out-of-core frameworks often assume discrete memory spaces (Host RAM vs. Device VRAM) and rely on explicit \texttt{cudaMemcpy} operations, which are redundant on UMA systems.
Furthermore, generic I/O libraries fail to leverage the specific access patterns of quantum gates, where the stride of memory access grows exponentially with the target qubit index.
This results in inefficient I/O operations and poor cache locality.
Therefore, optimizing quantum simulation on edge devices requires a specialized memory management strategy that exploits the zero-copy capabilities of UMA while efficiently handling the sequential yet strided access patterns inherent to quantum algorithms.
\EdgeQuantum overcomes these limitations by implementing a UVM-aware asynchronous pipeline that integrates efficient I/O scheduling with zero-copy computation, ensuring high performance even when the state vector far exceeds physical memory capacity.